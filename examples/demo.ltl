// examples/demo.ltl — runnable demo for ltl_tree_view

import std::io
use ltl_tree_view as lib

fn main() {
    io::println("ltl_tree_view demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}